function name(param1, param2) {
    var hello = "value";
    var test = {
        key: hello,
        secondKey: {
            nestedKey: "nestedValue"
        }
    }
    print(hello)
}

name(0.6, "", "", "")
